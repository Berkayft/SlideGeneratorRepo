<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Slides Hub</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://unpkg.com/tailwindcss@2.2.4/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* Hover animation for More Info button */
        .more-info {
            transition: color 0.3s ease;
        }
        .more-info:hover {
            color: #4ade80; /* Light green color */
        }

        html, body {
            background-color: #e6e6e6;
        }

        /* Description styling */
        .description {
            display: -webkit-box;
            -webkit-line-clamp: 2; /* Show two lines */
            -webkit-box-orient: vertical;
            overflow: hidden;
            margin-bottom: 1rem; /* Spacing between description and value */
        }

        /* Card structure adjustments */
        .card {
            display: flex;
            flex-direction: column;
            justify-content: space-between; /* Space content evenly */
            height: 100%; /* Card height */
        }

        /* Content area minimum height */
        .content-area {
            flex-grow: 1; /* Allow content to grow */
            display: flex;
            flex-direction: column;
            justify-content: space-between; /* Space content evenly */
        }
    </style>
</head>
<body class="relative">
    <%- include(navbar); %>

    <div class="container mx-auto px-4 flex flex-col mt-12">
        <h1 class="text-4xl font-bold text-left mt-10 mb-10">Most Viewed Slides</h1>
    </div>

    <!-- Slide Cards Section -->
    <div class="container mx-auto px-4">
        <section class="text-gray-700 font-bold">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <% slides.forEach((slide) => { %>
                    <div class="border-2 border-gray-200 bg-gray-100 font-bold border-opacity-60 rounded-lg overflow-hidden flex flex-col justify-between card">
                        <img class="lg:h-48 md:h-36 w-full object-cover object-center" src="slaid_image/2.jpeg" alt="Slide Image"/>
                        <div class="p-4 flex-grow content-area">
                            <h2 class="text-sm font-bold text-green-500 uppercase">Slide Title</h2>
                            <h1 class="text-lg font-medium text-gray-800 mt-2"><%= slide.title %></h1>
                            <h2 class="text-sm font-bold text-green-500 uppercase mt-4">Description</h2>
                            <p class="text-gray-800 mt-2 description"><%= slide.description %></p>
                            <h2 class="text-sm font-bold text-green-500 uppercase mt-4">View Count</h2>
                            <p class="text-lg font-medium text-gray-800 mt-2"><%= slide.viewedCount %></p>
                        </div>
                        <div class="p-4">
                            <a href="/moreInfo/<%= slide.filepath %>" class="text-gray-500 inline-flex items-center hover:text-gray-700">More Info
                                <svg class="w-4 h-4 ml-2" viewBox="0 0 24 24" stroke="currentColor" fill="none">
                                    <path d="M5 12h14"></path>
                                    <path d="M12 5l7 7-7 7"></path>
                                </svg>
                            </a>
                        </div>
                        
                    </div>
                <% }); %>
            </div>
        </section>
    </div>
    


    <%- include('footer') %>
</body>
</html>
